# SPDX-FileCopyrightText: 2026 Dirk Wahrheit
# SPDX-License-Identifier: Apache-2.0
## espos_sk component manifest: registry metadata for signalk-espos/espos_sk.
## The version is espOS's (version.txt) and moves with it, see docs/releasing.md.
# x-release-please-start-version
version: "0.8.0"
# x-release-please-end
description: "SignalK: mDNS discovery, access token, delta stream in and out"
url: "https://github.com/signalk-espOS/espOS/tree/main/components/espos_sk"
repository: "https://github.com/signalk-espOS/espOS.git"
documentation: "https://github.com/signalk-espOS/espOS/blob/main/docs/signalk.md"
issues: "https://github.com/signalk-espOS/espOS/issues"
license: "Apache-2.0"
maintainers:
  - "Dirk Wahrheit <dirkwahrheit@gmail.com>"
tags:
  - signalk
  - marine
  - esp-idf
  - espos
  - websocket
  - mdns
  - delta
files:
  exclude:
    - "build/**/*"
    - "build-*/**/*"
    - "sdkconfig*"
    - "managed_components/**/*"
    - "dependencies.lock"
    - "examples/*/build/**/*"
    - "examples/*/build-*/**/*"
    - "examples/*/managed_components/**/*"
    - "examples/*/dependencies.lock"
    - "examples/*/sdkconfig*"
    - "examples/*/*.pem"
dependencies:
  idf: ">=6.0.0,<6.1.0"
  # Sibling espOS components. override_path makes an in-tree build (and a
  # firmware that vendors espOS as a submodule) use the checkout next to this
  # file instead of downloading espos/<name> from the registry; a copy fetched
  # from the registry has no such directory and the field is ignored there.
  signalk-espos/espos_config:
    # x-release-please-start-version
    version: "^0.8.0"
    # x-release-please-end
    override_path: "../espos_config"
  signalk-espos/espos_httpd:
    # x-release-please-start-version
    version: "^0.8.0"
    # x-release-please-end
    override_path: "../espos_httpd"
  signalk-espos/espos_net:
    # x-release-please-start-version
    version: "^0.8.0"
    # x-release-please-end
    override_path: "../espos_net"
  signalk-espos/espos_health:
    # x-release-please-start-version
    version: "^0.8.0"
    # x-release-please-end
    override_path: "../espos_health"
  # cJSON, exact-pinned like every other registry dependency here.
  espressif/cjson: "==1.7.19~2"
  # espressif/mdns is espos_net's dependency (it owns the responder);
  # discovery browses through it and gets mdns.h via espos_net's REQUIRES.
  signalk-espos/espos_time:
    # x-release-please-start-version
    version: "^0.8.0"
    # x-release-please-end
    override_path: "../espos_time"
  signalk-espos/espos_event:
    # x-release-please-start-version
    version: "^0.8.0"
    # x-release-please-end
    override_path: "../espos_event"
